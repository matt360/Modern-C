// Static

// static variable in a class remains the same across the instances of the class
// which means that if I were to change the static variable it's going to be changed for all the instances of the class.