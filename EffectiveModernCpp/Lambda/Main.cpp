#include <iostream>
#include <string>
#include <vector>

// Function objects created through lambda expressions are known as closures.

enum class Color {
	Yellow, Red, Blue
};

int main()
{



	std::cin.get();
}