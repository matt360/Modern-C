#include <iostream> // use angular branckets only for compiling include paths; for the include directories or something that is declarted outside Visual Studio
#include "Log.h"    // use quotes for things that are in the Visual Studio project. you could go: "../Log.h" because it is relevant to the current directory

#include <iostream> // C++ files (no extension)
#include <stdio.h>  // C files (.h extension)